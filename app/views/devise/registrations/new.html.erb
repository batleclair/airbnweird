<h2>Sign up</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), data: {turbo: false}) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "email" }%>
    <%= f.input :password,
                required: true,
                hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :password_confirmation,
                required: true,
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :name,
               required: true%>
    <%= f.input :photo, as: :file %>
    <%= f.input :pathology%>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign up", class: "btn btn-primary" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>

<form data-turbo="false" class="simple_form new_user" id="new_user" novalidate="novalidate" action="/users" accept-charset="UTF-8" method="post" style="
    width: 50vw;
    margin: 0 25vw;
    border: 2px solid blueviolet;
    padding: 5px;
    border-radius: 14px;
"><input type="hidden" name="authenticity_token" value="fnV-XJOhr1WI6NCaIGtyhLyOC07ohlzVSSKiZUCFO1iNQsBEyFDXUaiBjl0-CocSvoQwStMLRa2FSmG7naDEHA" autocomplete="off">
